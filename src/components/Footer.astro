<footer>
    <p class="weee">Welcome to my footer!</p>
</footer>

<style>
    footer {
        background-color: var(--background-color-darker);
        height: 14rem;
        padding: 2rem 20%;
        
        display: flex;
        justify-content: space-around;
    }

    /* Wow fun animation! */
    .weee {
        width: max-content;
        transform: rotateY(0deg);
        transform-style: preserve-3d;
    }

    footer:hover .weee {
        color: rgb(247, 96, 96);
        animation:
            spin-horizontal 2.5s infinite linear,
            rainbow 1.5s infinite linear;
    }

    @keyframes spin-horizontal {
        0% {
            transform: rotateY(0deg);
        }
        100% {
            transform: rotateY(360deg);
        }
    }

    @keyframes rainbow {
        0% {
            filter: hue-rotate(0deg);
        }
        100% {
            filter: hue-rotate(360deg);
        }
    }
</style>